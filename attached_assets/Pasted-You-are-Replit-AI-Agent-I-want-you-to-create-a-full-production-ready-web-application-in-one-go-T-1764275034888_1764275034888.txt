You are Replit AI Agent. I want you to create a full, production-ready web application in one go.

TITLE: AI PDF LIBRARY BOT WITH ADMIN + USER SYSTEM

------------------------------------
SYSTEM REQUIREMENTS
------------------------------------
Build a full-stack web application with:

• Frontend: React (Vite or Next.js — choose best)
• Backend: Node.js + Express
• Database: MongoDB (use mongoose)
• Authentication: JWT + bcrypt
• File Uploads: Multer (store PDFs in /uploads)
• AI Search API: Local fuzzy matching (no external API needed)
• Chatbot Logic: Node backend API endpoint

The app should include:

------------------------------------
1. USER SIDE FEATURES
------------------------------------
• User login & signup (JWT authentication)
• User dashboard with:
   - Profile info (name, email)
   - Previous downloaded PDFs
   - Total downloads
• AI Chatbot interface:
   User types things like:
   - "I want Engineering Physics book"
   - "Give me C programming pdf"
   - "Provide physics note"
   - Misspellings should still match (use fuzzy matching)
• AI chatbot should:
   - Search only within PDFs uploaded by Admin
   - Find closest title using similarity scoring
   - Return best match + download link
   - Also show similar matches
   - If no PDF, check if admin added an external link
   - If still no match → return "Not found in library"
• When user downloads a PDF:
   - Log into user history (pdfId, timestamp)

------------------------------------
2. ADMIN SIDE FEATURES
------------------------------------
Admin login page (separate route)

Admin dashboard that includes:

A) Upload PDF
   - Upload PDF file
   - Title
   - Author
   - Category
   - Description
   - Tags/keywords
   - Auto detect file size

B) Add External Links
   - Book title
   - Official/alternate link
   - Description

C) Manage Library
   - List all PDFs
   - Edit / Delete
   - Replace PDF file
   - Update metadata

D) Users Management
   - List all users
   - Show profile
   - View download history
   - Total downloaded count

------------------------------------
3. FOLDER & FILE STRUCTURE
------------------------------------
Create a complete project structure:

/client
   /src
      components/
      pages/
      chatbot/
      admin/
      user/
   package.json

/server
   /controllers
   /models
   /routes
   /middleware
   /utils
   uploads/
   package.json

.env.example

------------------------------------
4. AI CHATBOT LOGIC (IMPORTANT)
------------------------------------
AI = Not external.
You must implement fuzzy search yourself in backend:

Use:
- Fuse.js OR your custom similarity scoring method

Bot Workflow:
1. User sends a message → POST /api/chat
2. Backend extracts keywords → fuzzy search in DB
3. Respond with:
   {
      bestMatch: {title, description, pdfUrl, size},
      similar: [...],
      externalLink: {if available}
   }

------------------------------------
5. WHAT YOU MUST GENERATE
------------------------------------
✔ All backend code  
✔ All frontend code  
✔ All components  
✔ All pages  
✔ All routes  
✔ All models (User, PDF, DownloadHistory, ExternalLink)  
✔ All controllers  
✔ Chatbot API endpoint  
✔ Authentication system  
✔ File upload system  
✔ Dashboard UIs  
✔ User UIs  
✔ Chatbot UI  
✔ Everything needed to run the project  

------------------------------------
6. DEPLOYMENT REQUIREMENTS
------------------------------------
• Must run locally on Replit without errors
• Must include "npm install" instructions automatically
• Start both frontend and backend concurrently
• Add README.md with setup steps
• Provide test data if needed

------------------------------------
7. FIRST ACTION
------------------------------------
Immediately begin generating the full project:
• Create all files
• Fill them with complete working code
• Build the entire app end-to-end
• Then run the project

Do NOT ask any questions. Build the full bot now.
